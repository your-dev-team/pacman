class Ghost {
  constructor(sprite) {
    this.sprite = sprite;
  }

  move() {
    // Move the ghost
  }

  catchPlayer() {
    // Handle the ghost catching the player
  }
}

module.exports = Ghost;
